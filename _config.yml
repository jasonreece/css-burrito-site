# Site settings
title: css-burrito
description: > # this means to ignore newlines until "baseurl:"
  css-burrito - a customizable Sass template and generator

baseurl: "" # the subpath of your site, e.g. /blog/

# Build settings
markdown: kramdown

exclude: ["node_modules", "gulp", "gulpfile.js", "package.json"]
